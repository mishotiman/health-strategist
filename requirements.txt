# Web
fastapi
uvicorn[standard]
pydantic-settings
python-multipart  # FastAPI file uploads

# Database
psycopg[binary]
pgvector

# LLM + embeddings (used from Week 1 Wed onward)
anthropic
voyageai
openai  # fallback embeddings provider
tenacity  # retry/backoff for bulk embedding + Europe PMC fetch

# PDF / ingestion (used from Week 1 Tue onward)
pymupdf
pyyaml  # data/corpus_topics.yml — the corpus acquisition recipe

# Eval harness (Week 2)
langsmith

# Agent (Week 4)
langgraph
langchain-anthropic

# Auth: accounts, external sign-in, transactional email
argon2-cffi          # argon2id password hashing
authlib              # Google/Microsoft OIDC (discovery, JWKS, PKCE, nonce)
resend               # verification + password-reset email

# Tests
pytest
